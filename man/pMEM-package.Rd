% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pMEM-package.R
\name{pMEM-package}
\alias{pMEM-package}
\title{pMEM: Predictive Moran's Eigenvector Maps for Spatial Modeling}
\description{
\code{pMEM} implements Predictive Moran's Eigenvector Maps, a method for
spatially-explicit prediction of environmental variables using eigen-
decomposition of distance-based spatial weighting matrices.
}
\details{
\subsection{Package Overview}{
  pMEM extends classical Moran's Eigenvector Maps (MEM) by:
  \itemize{
    \item Enabling prediction at unsampled locations via \code{predict()}
          methods
    \item Supporting tunable distance weighting functions (exponential,
          Gaussian, power, linear, spherical, hyperbolic, hole_effect)
    \item Optimizing spatial scale parameters via cross-validated model
          selection
    \item Integrating with regression frameworks (lm, glm, glmnet)
    \item Providing fast C++ backend via Rcpp for efficient eigenvector
          selection
  }
}

\subsection{Main Functions}{
  Core workflow functions:
  \itemize{
    \item \code{\link{genDistMetric}}: Generate distance metric functions
          (symmetric Euclidean or asymmetric complex-valued)
    \item \code{\link{genDWF}}: Generate distance weighting functions
          (7 built-in types with tunable parameters)
    \item \code{\link{genSEF}}: Generate Spatial EigenMap (SEMap) objects
    \item \code{\link{getMinMSE}}: Fast eigenvector selection via
          cross-validated MSE
    \item \code{\link{predict.SEMap}}: Interpolate eigenfunctions at new
          locations
  }
}

\subsection{SEMap Class and Methods}{
  The \code{\link{SEMap-class}} encapsulates spatial eigenfunctions with
  methods:
  \itemize{
    \item \code{print()}: Display object summary (sites, components,
          eigenvalues)
    \item \code{as.matrix()}: Extract eigenvectors as numeric/complex matrix
    \item \code{as.data.frame()}: Extract eigenvectors as data frame
    \item \code{predict()}: Calculate eigenfunction scores at new coordinates
  }
}

\subsection{Included Datasets}{
  Two spatial ecological datasets for examples and testing:
  \itemize{
    \item \code{\link{salmon}}: Atlantic salmon parr abundance and habitat
          variables along a 1520 m river transect (76 sites, 5 variables)
    \item \code{\link{geoMite}}: Oribatid mite community data with GIS layers
          from a peat bog (70 cores, 35 species, 5 polygon layers)
  }
}

\subsection{Typical Workflow}{
  Standard spatial modeling workflow with pMEM:
  \enumerate{
    \item Load coordinates and response variable(s)
    \item Generate distance metric: \code{m <- genDistMetric()}
    \item Generate weighting function: \code{f <- genDWF("Gaussian", range = 50)}
    \item Create SEMap object: \code{sef <- genSEF(x, m, f)}
    \item Split data into training/testing sets
    \item Select optimal eigenvectors: \code{res <- getMinMSE(U, y, Up, yy)}
    \item Fit model with selected eigenvectors: \code{lm(y ~ ., data = sef[, sel])}
    \item Predict at new locations: \code{predict(sef, newdata, wh = sel)}
  }
}

\subsection{Complex-Valued (Asymmetric) Metrics}{
  pMEM supports directional spatial processes via complex-valued distance
  metrics:
  \itemize{
    \item Use \code{genDistMetric(delta)} with non-zero \code{delta}
          parameter
    \item For 1D transects: \code{delta} sets phase shift for
          upstream/downstream
    \item For 2D data: \code{theta} sets influence angle (direction of flow)
    \item Eigenfunctions have real and imaginary parts representing
          directionality
    \item Useful for rivers, wind patterns, ocean currents, and other
          directed processes
  }
}

\subsection{Performance Notes}{
  \itemize{
    \item \code{getMinMSE()} is implemented in C++ via Rcpp (~50× faster than
          pure R)
    \item Memory-efficient handling of large eigenvector matrices
    \item Parallelization-ready architecture for future extensions
  }
}

\subsection{References}{
  Key methodological references:
  \itemize{
    \item Guénard, G. and Legendre, P. (2024). Spatially-explicit predictions
          using spatial eigenvector maps. \emph{Methods in Ecology and Evolution}.
          <doi:10.1111/2041-210X.14413>
    \item Dray, S., Legendre, P., and Peres-Neto, P.R. (2006). Spatial modelling:
          A comprehensive framework for Principal Coordinate Analysis of
          Neighbour Matrices (PCNM). \emph{Ecological Modelling}, 196: 483–493.
          <doi:10.1016/j.ecolmodel.2006.02.015>
    \item Moran, P.A.P. (1948). The interpretation of statistical maps.
          \emph{Journal of the Royal Statistical Society B}, 10: 243–251.
          <doi:10.1111/j.2517-6161.1948.tb00012.x>
  }
}
}
\examples{
## Quick start: model channel depth along a river transect
data("salmon", package = "pMEM")

## Split into training/testing sets:
set.seed(123)
test <- sample(nrow(salmon), 25)
train <- setdiff(seq_len(nrow(salmon)), test)

## Generate spatial eigenfunctions:
sef <- genSEF(
  x = salmon$Position[train],
  m = genDistMetric(),
  f = genDWF("Gaussian", range = 50)
)

## Select optimal eigenvectors via cross-validated MSE:
res <- getMinMSE(
  U = as.matrix(sef),
  y = salmon$Depth[train],
  Up = predict(sef, salmon$Position[test]),
  yy = salmon$Depth[test],
  complete = FALSE
)

## Coefficient of prediction (R² on test set):
1 - res$mse / var(salmon$Depth[test])
#> [1] 0.8108639

## View SEMap object summary:
sef
#> A SEMap-class object
#> --------------------
#> Number of sites: 51
#> Directional: no
#> Number of components: 50
#> Eigenvalues: 3.76546,3.54744,3.43502,...,0.00066,0.00012
#> --------------------

## More examples:
vignette("Using_pMEM", package = "pMEM")

}
\author{
\packageAuthor{pMEM}
}
